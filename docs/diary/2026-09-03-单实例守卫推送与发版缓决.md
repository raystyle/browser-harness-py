# 2026-09-03 单实例守卫推送与发版缓决

Issue #1 收尾日：三笔提交推 `origin/main`（fix M109 / docs 回写 / gitignore `.ohmyagents/`），Issue #1 回帖（根因三层 + 修复方案 + 验证 + 升级过渡注意）并以 completed 关闭。用户定向：**暂不封版**——v0.6.7 不发，单实例守卫先在 main 沉淀，待后续功能/修复聚合发版（与 09-01 自省「版本粒度过细」的教训同向）。TODO 队列挂「v0.6.7 发版」排后。

## 自省

- **昨日三处弯路同族归纳**：`BU_HOME`/`BH_HOME` 前缀写错、SKILL.md 三副本同步方向搞反（用根副本覆盖了自己刚改的 src）、bash `&` 把整条 `&&` 链背景化致变量丢失。三件事一个根：**对环境的先验假设没核对就动手**。共因预防：动隔离变量前先对照 `paths.py` 的真实前缀；改多副本文件先确认权威源与分发方向（本仓是根 SKILL.md → src/skills）；shell 组合命令先想清操作符优先级再写脚本。SKILL 那次是三副本一致性单测当场救场（红了才发现覆盖方向反了）——R003 副本一致性测试的价值有了活例。[实证: 2026-09-02 本机]
- **测试环境 ≠ 真环境**：跨进程锁单测首版按 `Popen.pid` 断言 + `p.kill()` 回收，uv run 下 `sys.executable` 是 trampoline，pid 对不上、kill 也不达持锁进程。[实证: 2026-09-02 首版红] 预防：写涉及子进程的测试前，先花一条命令验明执行身份（`sys.executable` 到底是谁）。
- **验证诚实度**：守卫的真栈「健康拉起」路径（ensure_daemon → 新 `__main__` → 连上真浏览器）未做完整 E2E——本 checkout 的 dev `.env` 是 WSL 侧配置（9224 + `/home` 路径），Windows 侧拉起会造脏 profile，故只做到结构覆盖 + 争抢三分支实测。[记忆: 待首次真栈拉起复核] 下次 x-monitor 或 CLI 浏览器作业的首启即是活体验证，留意 daemon 日志的锁行为与单实例表现。
- **回帖承诺与发版缓决的耦合**：Issue #1 回帖写了「随下个 Release 经 `--update` 下发」，发版缓决后目标机吃到修复的时间点后移——这是缓决的已知代价，不是遗漏；TODO 队列已挂，不需要额外催更。

## Issue #2 双模式对齐（同日追加）

第二条 Issue：heredoc 管道里 `web_fetch` NameError 而 `google_search` 可用。根因不是缺能力——web-fetch 的核心 `extract_url_content` 本就预导入——是「app 名 ↔ 函数名」映射靠运气：核心写在 agent_helpers 的（google/bing-search）有同名函数，写成纯 CLI 包装的（web-fetch）没有，核心在打包模块的（x-search）完全不可达。

用户定向把修复抬成原则：**stdin 模式与命令模式双兼容是 harness 类程序运行的核心逻辑**。落地三件：`run_app(name, *args, json_output)` 通用子命令桥（子进程复刻 CLI 语义，核心子命令与 app 通吃，按构造对齐、永不漂移）；`web_fetch()` 别名对齐 CLI 默认值（纯 HTTP 起步、bot 墙升级浏览器）；R003 §2 落「双模式兼容」条款（app 核心进 agent_helpers、脚本保持薄包装、能力不允许单边存在）。+5 单测（含复刻 issue 踩空链路的 `import *` 冒烟），227 绿。

自省：真栈 E2E 依旧欠——连续两条 Issue 都卡在「本 checkout dev `.env` 是 WSL 侧配置」上（Windows 侧跑 stdin 模式会以 `/home` 路径 profile 拉 Chrome 造脏目录），今日以 import 链冒烟 + 命令构造单测代偿，真栈验证挂到发版自验。环境反复挡路就该沉淀：TODO 挂「dev 环境 Windows 侧真栈姿势」（BH_HOME 指独立目录 + .env 平台分文件）。

## v0.6.7 发版（同日三追，用户定向解缓决）

上午挂的「暂不封版」下午被用户指令解除：发 0.6.7，M109 + Issue #2 随版。材料走 R003 §6 单：pyproject 0.6.7 + CHANGELOG 两条 + README 五处（预导入助手补 `web_fetch`/`run_app`、环境变量补 `BH_LOCK_GRACE`、测试计数 227、M101-M109、skills 样例 8 apps——v0.6.6「只更 SKILL 漏 README」的复检清单这次兑现）；预检四件套全绿（227 单测 / symlink 零 / 树净 / 版本就绪）→ commit → push → tag v0.6.7 → Release「daemon 单实例守卫与管道命令双模式对齐」。

自验链路全过：`--update` 接力升级 0.6.6→0.6.7（skills 三落点铺装、尾部输出落地版本、M108 这次未犯，API 正常见新版）；doctor 绿；stdin 真栈 `run_app("browsers")` + `web_fetch(example.com)` 过冷启动 daemon——顺带把上午挂的「守卫健康拉起活体验证」[记忆] 项闭环：锁行为干净、无僵尸。

自省（发版自验的两个假警报）：doctor 间歇 FAIL 两次都是误读——「active browser connections 0」是闲置稳态（SKILL 语义：干活时为 1）；「daemon not alive」是撞上 ensure_daemon 对 stale daemon 的自愈重启窗口。进程表里两个 `-m browser_harness.daemon` 也是假象：uv venv 的 trampoline 父子对（venv `Scripts\python.exe` 跳板 → 基础解释器真身，09-02 单测踩过同族坑）。[实证: 2026-09-03 发版机] 教训：判 daemon 状态先对 pid 文件 + 监听端口 + 父子树三件套，别拿进程计数吓自己。

下一目标（用户定向，TODO 已挂）：agent-workspace 更名 browser-workspace。
