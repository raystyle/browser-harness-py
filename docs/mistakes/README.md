# mistakes

错误速查（M 编号：分类文件 M1xx、行级 M0xx）。现有：M101（profile 污染）、M102（升级锁 venv）、M103（uninstall 删 shim）、M104（Linux Chrome 重写 cmdline）、M105（单测字符串比对路径在 win32 失效）、M106（symlink 条目在 Windows 编辑后目标变成整份正文）、M107（CRLF 检出致 skills 哈希永不相等）、M108（匿名 API 限流 403 被误报为 up to date）、M109（daemon 无单实例守卫致多实例并存），见 `INDEX.md` 第五节。命名与登记见 `INDEX.md` 与 `docs\guide\G001-*.md`。
